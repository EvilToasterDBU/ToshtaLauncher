name: Build Application and Make Release

on:
  push:
    tags:
      - '*'

jobs:

  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    with:
      build_type: Release
      is_qt_cached: false
    secrets:
      SPARKLE_ED25519_KEY: ${{ secrets.SPARKLE_ED25519_KEY }}
      WINDOWS_CODESIGN_CERT: ${{ secrets.WINDOWS_CODESIGN_CERT }}
      WINDOWS_CODESIGN_PASSWORD: ${{ secrets.WINDOWS_CODESIGN_PASSWORD }}
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}

  create_release:
    needs: build_release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          submodules: 'true'
          path: 'ToshtaLauncher-source'
      - name: Download artifacts
        uses: actions/download-artifact@v3
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/ToshtaLauncher-source ToshtaLauncher-${{ env.VERSION }}
          mv ToshtaLauncher-Linux-Qt6-Portable*/ToshtaLauncher-portable.tar.gz ToshtaLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv ToshtaLauncher-Linux-Qt6*/ToshtaLauncher.tar.gz ToshtaLauncher-Linux-Qt6-${{ env.VERSION }}.tar.gz          
          mv ToshtaLauncher-Linux-Portable*/ToshtaLauncher-portable.tar.gz ToshtaLauncher-Linux-Portable-${{ env.VERSION }}.tar.gz
          mv ToshtaLauncher-Linux*/ToshtaLauncher.tar.gz ToshtaLauncher-Linux-${{ env.VERSION }}.tar.gz
          mv ToshtaLauncher-*.AppImage/ToshtaLauncher-*.AppImage ToshtaLauncher-Linux-${{ env.VERSION }}-x86_64.AppImage
          mv ToshtaLauncher-macOS-Legacy*/ToshtaLauncher.tar.gz ToshtaLauncher-macOS-Legacy-${{ env.VERSION }}.tar.gz
          mv ToshtaLauncher-macOS*/ToshtaLauncher.tar.gz ToshtaLauncher-macOS-${{ env.VERSION }}.tar.gz

          tar --exclude='.git' -czf ToshtaLauncher-${{ env.VERSION }}.tar.gz ToshtaLauncher-${{ env.VERSION }}

          for d in ToshtaLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="ToshtaLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv ToshtaLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in ToshtaLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="ToshtaLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv ToshtaLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          name: Prism Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          files: |
            ToshtaLauncher-Linux-${{ env.VERSION }}.tar.gz
            ToshtaLauncher-Linux-Portable-${{ env.VERSION }}.tar.gz
            ToshtaLauncher-Linux-${{ env.VERSION }}-x86_64.AppImage
            ToshtaLauncher-Linux-Qt6-${{ env.VERSION }}.tar.gz
            ToshtaLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            ToshtaLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            ToshtaLauncher-Windows-MSVC-Legacy-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MSVC-Legacy-Portable-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MSVC-Legacy-Setup-${{ env.VERSION }}.exe
            ToshtaLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            ToshtaLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            ToshtaLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            ToshtaLauncher-macOS-${{ env.VERSION }}.tar.gz
            ToshtaLauncher-macOS-Legacy-${{ env.VERSION }}.tar.gz
